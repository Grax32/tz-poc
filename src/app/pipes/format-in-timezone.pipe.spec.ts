import { FormatInTimezonePipe } from './format-in-timezone.pipe';

describe('FormatInTimezonePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInTimezonePipe();
    expect(pipe).toBeTruthy();
  });
});
